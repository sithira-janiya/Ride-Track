# AGENTS

This project is a modular Expo app scaffold.

## Guidelines

- Keep feature folders grouped by domain, such as `Auth` and `Home`.
- Prefer named exports for reusable modules.
- Use placeholder implementations that are ready to expand into real logic.
- Keep Redux, navigation, and screens decoupled through module boundaries.
