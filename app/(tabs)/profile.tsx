import { Ionicons } from '@expo/vector-icons';
import { useState } from 'react';
import { Pressable, SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useDispatch, useSelector } from 'react-redux';
import { RootState } from '../../store/store';
import { setRole } from '../../store/slices/authSlice';
export default function Profile() { const dispatch = useDispatch(); const auth = useSelector((state: RootState) => state.auth); const [expanded, setExpanded] = useState(false); return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Text style={styles.kicker}>ACCOUNT</Text><Text style={styles.heading}>Profile</Text><View style={styles.identity}><View style={styles.avatar}><Text>{auth.avatar}</Text></View><View><Text style={styles.name}>{auth.name}</Text><Text style={styles.email}>maya.delacruz@email.com</Text></View><Ionicons name="checkmark-circle" size={20} color="#2A9D8F" /></View><Text style={styles.label}>VIEW AS</Text><View style={styles.roleRow}>{(['commuter', 'conductor', 'admin'] as const).map(role => <Pressable key={role} onPress={() => dispatch(setRole(role))} style={[styles.role, auth.role === role && styles.selected]}><Ionicons name={role === 'commuter' ? 'person' : role === 'conductor' ? 'bus' : 'stats-chart'} size={17} color={auth.role === role ? '#F8F5EF' : '#526B66'} /><Text style={[styles.roleText, auth.role === role && styles.selectedText]}>{role}</Text></Pressable>)}</View><View style={styles.menu}>{[['person-outline', 'Personal details'], ['card-outline', 'Payment methods'], ['notifications-outline', 'Notifications'], ['help-circle-outline', 'Help center']].map(([icon, text]) => <Pressable key={text} style={styles.menuItem} onPress={() => setExpanded(!expanded)}><View style={styles.menuLeft}><Ionicons name={icon as keyof typeof Ionicons.glyphMap} size={20} color="#4F706A" /><Text style={styles.menuText}>{text}</Text></View><Ionicons name="chevron-forward" size={17} color="#9AA9A4" /></Pressable>)}</View><Text style={styles.version}>RIDETRACK • VERSION 1.0.0</Text></ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#F8F5EF' }, content: { padding: 22 }, kicker: { color: '#E76F51', fontSize: 10, fontWeight: '800', letterSpacing: 1.2 }, heading: { color: '#163B3E', fontSize: 28, fontWeight: '800', marginTop: 5 }, identity: { backgroundColor: '#FFFDF9', borderRadius: 15, padding: 17, flexDirection: 'row', alignItems: 'center', gap: 12, marginTop: 24, borderWidth: 1, borderColor: '#E1E8E2' }, avatar: { width: 48, height: 48, borderRadius: 24, backgroundColor: '#E76F51', justifyContent: 'center', alignItems: 'center' }, name: { color: '#163B3E', fontSize: 15, fontWeight: '800' }, email: { color: '#82918D', fontSize: 11, marginTop: 4 }, label: { color: '#84928E', fontSize: 10, fontWeight: '800', letterSpacing: 1.1, marginTop: 27, marginBottom: 10 }, roleRow: { flexDirection: 'row', gap: 7 }, role: { flex: 1, height: 42, borderRadius: 10, backgroundColor: '#E7EFEB', alignItems: 'center', justifyContent: 'center', flexDirection: 'row', gap: 5 }, selected: { backgroundColor: '#0E3B43' }, roleText: { color: '#526B66', fontSize: 11, fontWeight: '700', textTransform: 'capitalize' }, selectedText: { color: '#FFFDF9' }, menu: { marginTop: 23, backgroundColor: '#FFFDF9', borderRadius: 15, paddingHorizontal: 15, borderWidth: 1, borderColor: '#E1E8E2' }, menuItem: { minHeight: 57, borderBottomWidth: 1, borderBottomColor: '#EDF0EB', flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, menuLeft: { flexDirection: 'row', alignItems: 'center', gap: 13 }, menuText: { color: '#385B57', fontWeight: '700', fontSize: 13 }, version: { textAlign: 'center', color: '#A5B0AC', fontSize: 10, marginTop: 27, letterSpacing: 1 } });
